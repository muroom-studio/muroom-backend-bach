spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:

  sql:
    init:
      mode: never
  #  data:
  #    redis:
  #      host: localhost
  #      port: 6379

  jpa:
    open-in-view: false
    hibernate:
      # ddl-auto: update
      ddl-auto: none
    properties:
      hibernate:
        #show-sql: true
        format_sql: true

cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket: test-bucket
      presign:
        expiration: 10m
    stack:
      auto: false

jwt:
  secret-key: some-very-very-very-very-very-very-very-long-secret-key
  expiration-ms: 900000 #15분

logging:
  level:
    org.springframework: INFO
    com.moty.solarserver: DEBUG

seoul:
  api:
    key: test-seoul-api-key

juso:
  api:
    search-key: test-juso-search-api-key
    coord-key: test-juso-coord-api-key

kakao:
  api:
    rest-api-key: test-kakao-rest-api-key

server:
  url: http://localhost:8080
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false


oauth2:
  kakao:
    client-id: test-kakao-client-id
    client-secret: test-kakao-client-secret
  google:
    client-id: test-google-client-id
    client-secret: test-google-client-secret